[project]
name = "pytorch-e2e"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.7.0",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]
dev = [
    "editables>=0.5",
    "hatchling>=1.29.0",
]
