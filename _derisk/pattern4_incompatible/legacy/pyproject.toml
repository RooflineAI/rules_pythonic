[project]
name = "legacy-workspace"
version = "0.1.0"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["packages/*", "../shared/core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"
